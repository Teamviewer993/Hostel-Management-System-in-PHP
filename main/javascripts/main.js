$(document).ready(function() {
    $('.sidenav').sidenav();
    //    Full Width Slider Coding
    $('.carousel.carousel-slider').carousel({
        fullWidth: true,
        indicators: true
    });
    $('.materialboxed').materialbox();
});